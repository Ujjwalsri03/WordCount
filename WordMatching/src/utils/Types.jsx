export const ItemGroup = Set;

// Define StatusOptions as an object to simulate an enum
export const StatusOptions = {
  Success: 'success',
  Failure: 'failure',
};

// Define Status as a value from StatusOptions or null
export const Status = null;